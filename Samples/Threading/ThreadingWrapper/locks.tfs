namespace TaffyScript.Threading.Locks {
    import Locks.Lock(object) as _lock;
    import Locks.Unlock(object) as _unlock;

    object moniter {
        event create {
            _key = [];
        }

        event lock {
            _lock(_key);
        }

        event unlock {
            _unlock(_key);
        }
    }
}