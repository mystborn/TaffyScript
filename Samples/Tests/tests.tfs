using UnitTest;

namespace LanguageTests {
    script main {
        var tests = new obj_unit_test("Operator Tests", 
                                      );
        tests.run_tests();
    }

    script addition_tests() {
        
    }
}